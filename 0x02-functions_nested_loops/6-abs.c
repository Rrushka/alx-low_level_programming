#include "main.h"

/* task 6 */

/**
 * _abs - start
 * num: the integer value
 * print absolute vaalue of an integer
 * Return: num
 */
int _abs(int num)
{
	if (num >= 0)
	{	
		return (num);
	}
	else
	{
		return (-num);
	}
}
