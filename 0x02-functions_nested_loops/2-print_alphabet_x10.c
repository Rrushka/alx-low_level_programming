#include "main.h"

/* print alphabet x10 */

/**
 * print_alphabet_x10 - start
 * only use _putchar twice
 * no returns because void return type
 */
void print_alphabet_x10(void)
{
	char alpha10;
	int n = 0;

	while (n < 10)
	{
	while (alpha10 = 'a'; alpha10 <= 'z'; alpha10++)
	{
		_putchar(alpha10);
	}
		_putchar('\n');
	}
}
